import Table from '../Table';
import TableAdd from '../TableAdd';
import Ticker from '../Ticker';
import { render } from '@testing-library/react';

test('should render AgGridReact component', () => {
  render(<Ticker />);
});
